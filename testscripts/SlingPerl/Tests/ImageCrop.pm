#!/usr/bin/perl

package Tests::ImageCrop;

#{{{imports
use strict;
use lib qw ( .. );
use Sling::ImageCrop;
use Test::More;
#}}}

#{{{sub run_regression_test

=pod

=head2 run_regression_test

Run regression tests for the image crop object.

=cut

sub run_regression_test {
    my ( $authn, $verbose, $log ) = @_;
}
#}}}

1;
